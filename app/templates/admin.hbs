<div class='master'>
  {{#each job in model}}
    <div class='job-list-item'>
      {{#link-to 'admin.job' this}}
        {{job.title}}
      {{/link-to}}
      at {{company.name}} | {{location}}
    </div>
  {{/each}}
</div>
<div class='detail'>
  {{outlet}}
</div>
